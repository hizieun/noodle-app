# 10. 일반 사용자 관점 개선 백로그 (2026-08-04)

> 실제 화면·데이터·사용자 여정을 근거로 뽑은 개선 목록. UX 재구성(docs/09)·리뷰(docs/07) 이후 "다음에 뭘 할까"의 기준.
> 우선순위: **P0=임팩트 큼/자주 겪음**, P1=가치 있음, P2=여유될 때. 노력(S/M/L) 병기.

---

## 진단 요약 (근거)

- **카드가 전부 텍스트+이모지** — `data.json`에 사진 필드 없음. 발견 앱의 핵심인 "먹음직스러움"이 화면에 없음. → 가장 큰 공백.
- **온보딩 부재** — "노포/야장"이 뭔지, 상단 토글이 뭘 바꾸는지 신규 사용자가 모름.
- **검색이 단순** — 이름·메뉴 문자열 매칭만. 최근검색·추천·메뉴 카테고리 원탭 없음.
- **안 쓰는 데이터** — `리뷰수`(신뢰도), `휴무일`(오늘 쉼 표시), `정보검증일`(신선도) 모두 미노출.
- **발견 피드 미완** — "지역별로 둘러보기"(구 칩) 레일이 기획(docs/09 B.5)에 있으나 미구현.

---

## P0 — 사용자가 매번 마주치고, 인상을 좌우

### 10.1 발견 피드·카드에 사진 (M~L)
문제: 카드가 텍스트라 "발견하는 재미"가 약함. 
- **10.1a (M)** — **리뷰 사진 재활용**: 커뮤니티 리뷰에 올라온 대표 사진 1장을 해당 식당 카드/레일 썸네일로. "사진 리뷰 있는 집" 레일 신설. UGC라 저작권 안전, 이미 인프라 있음. *단점: 초기엔 사진 있는 집이 적음(콜드스타트).*
- **10.1b (L)** — **크롤러 대표사진 수집**: 카카오맵 상세의 대표 이미지 URL을 크롤해 `photo_url` 필드 추가. 커버리지 즉시 100%. *리스크: 핫링킹/ToS·이미지 안정성 검토 필요. 별도 스파이크.*
- 권장: 10.1a 먼저(안전·저비용) → 데이터 쌓이며 자연 개선, 10.1b는 스파이크 후 결정.

### 10.2 첫 방문 온보딩 (S) — ✅ 완료 2026-08-05
- 최초 1회 웰컴 카드(`Onboarding.jsx`): 노포/야장 뜻 + 하단 탭·AI 사용법 3단계. `nopo-onboarded` localStorage로 1회성. 코치마크 대신 단일 카드(레이아웃 무관·견고).

### 10.3 "오늘" 영업 정보 강화 (S~M)
- `휴무일` 필드 활용: 카드/모달에 **"오늘 휴무"** 명시(지금 "영업종료"와 구분).
- 모달 영업시간에서 **오늘 요일 강조**.
- 신뢰 판단(J5) 직접 개선, 데이터 이미 있음.

---

## P1 — 가치 있는 개선

### 10.4 검색 경험 (M)
- **최근 검색어**(localStorage) + **메뉴 카테고리 칩**(국밥·고기·해장·전/부침·면 등 원탭 → 키워드 검색).
- 빈 결과 화면에 인기/추천 검색어 제안.

### 10.5 지역별로 둘러보기 레일 (S~M)
- 발견 피드에 구(區) 칩 → 그 동네 모음. docs/09 B.5 미완 조각. 기존 데이터로 규칙 생성.

### 10.6 지도 "이 지역 검색" (M)
- 지도 이동/줌 시 "보이는 영역만" 필터 + "이 지역 재검색" 버튼. 지도 탭을 진짜 탐색 도구로.

### 10.7 리뷰 신뢰·상호작용 (M)
- `리뷰수` 필드로 카드에 신뢰도 힌트(예: 리뷰 많은 집 배지).
- 커뮤니티 리뷰: **사진 있는 순** 정렬, **도움돼요/신고** 버튼(신고는 운영 안전).

---

## P2 — 여유될 때

### 10.8 재방문·개인화 (M)
- 방문 기록 타임라인(날짜별), 내 평가 통계("내가 별 5 준 집 N곳").
### 10.9 공유 강화 (S)
- 리스트 공유에 카카오 공유 SDK, 공유 미리보기 카드.
### 10.10 상태·오프라인 메시지 (S)
- 네트워크 오류·오프라인 시 명확한 안내(현재 PWA 캐시는 있음).

---

## 권장 착수 순서

1. **10.2 온보딩(S)** — 가장 싸고 신규 사용자 체감 큼. 빠른 선반영.
2. **10.1a 리뷰 사진 카드 노출(M)** — 발견 앱의 시각 공백을 UGC로 메움(안전).
3. **10.5 지역별 레일 + 10.3 오늘 영업(S~M)** — 데이터로 바로 되는 완성도.
4. 이후 10.4 검색 / 10.6 지도 재검색 / 10.1b 크롤 사진 스파이크.
